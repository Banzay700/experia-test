import styled from 'styled-components'

export const TableImageWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 16px;
  max-width: 160px;
  div {
    background-color: transparent;
  }
`

export const TableImage = styled.img`
  max-width: 28px;
  max-height: 40px;
`
